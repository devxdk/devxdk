# The signer source is checked out here by scrape-and-sign.yml at CI time only;
# it must never be committed back into the manifest repo.
/app-src/

# Local Python tooling cruft (scripts/devxdk_manifest).
__pycache__/
*.pyc
.pytest_cache/

# Local recipe-run outputs (CI legs upload these as workflow artifacts).
build/
